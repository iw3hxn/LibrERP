please attention:
If you want install "crm" module,
is required this module ("hr_agent") be installed first,
and then the  "crm" module.

ATTENZIONE:
Se si desidera installare il modulo "crm",
è necessario installare questo modulo ("hr_agent") per primo,
successivamente installare il modulo "crm".

